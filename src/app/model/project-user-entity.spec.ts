import { ProjectUserEntity } from './project-user-entity';

describe('ProjectUserEntity', () => {
  it('should create an instance', () => {
    expect(new ProjectUserEntity()).toBeTruthy();
  });
});
